# Encyclopedia index (local)

Run `npm run encyclopedia:build:sample` or `npm run encyclopedia:build` to generate:

- `index.json` — perceptual hashes per PDF page
- `thumbs/` — 320px grid thumbnails
- `previews/` — 1280px HD images for zoom/detail view

These files are gitignored because they are large and tied to your local PDF path.
